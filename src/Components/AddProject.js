import React from "react";

const AddProject = () => {
  // Implement your form and logic to add a new project
  return (
    <section className="add-project">
      <h2>Add New Project</h2>
      {/* Form to add new project */}
    </section>
  );
};

export default AddProject;
